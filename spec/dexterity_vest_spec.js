describe('DexterityVest', () => {
  describe('#updateQuality', () => {
    it('updates the sell_in and quality', () => {
      const dv = new DexterityVest(new Item('+5 Dexterity Vest', 10, 20))

      dv.updateQuality()

      expect()
    })
  })
})
